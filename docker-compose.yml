services:
  stt-server:
    build: .
    ports:
      - "7926:7926"
    volumes:
      - ./uploads:/app/uploads
      - ./.env:/app/.env
    environment:
      - FLASK_ENV=development
      - FLASK_DEBUG=True
    restart: unless-stopped 