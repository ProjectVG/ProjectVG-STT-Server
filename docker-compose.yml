services:
  stt-server:
    build: .
    ports:
      - "7950:8080"
    volumes:
      - ./uploads:/app/uploads
      - ./.env:/app/.env
    environment:
      - FLASK_ENV=development
      - FLASK_DEBUG=True
    restart: unless-stopped 